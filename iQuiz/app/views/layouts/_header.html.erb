  <header class="navbar navbar-fixed-top">
      <nav class="navbar-inner">
        <div class="container">
          <div class="nav pull-left">
            <li><%= link_to 'iQuiz', '/' %></li>
          </div>
          <div class="nav pull-right">
           
            <% if user_signed_in? %>
          
              <li><%= link_to 'Add +', new_question_path %></li>
          
            <% else %>
        
              <li><%= link_to 'Sign In', new_user_session_path %></li>

            <% end %>
         
          </div>
        </div>
      </nav>
    </header>